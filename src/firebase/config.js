// firebase/config.js — STUB (Firebase replaced with localStorage)
// This file is kept to avoid breaking any imports that reference it.
// No Firebase SDK is initialized here.

export const db = null;
export const auth = null;
export default null;
