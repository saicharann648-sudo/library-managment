// firebase/auth.js — STUB (all auth handled by localDB.js)
// Kept to avoid import errors in any legacy references.

export const loginUser = async () => { throw new Error("Use localDB loginUser instead"); };
export const logoutUser = async () => {};
export const onAuthChange = () => () => {};
